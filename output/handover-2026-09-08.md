---
type: handover
title: "Hermes Sentinel — เอกสารส่งมอบ 2026-09-08"
project: HMS-2026-001 Hermes Sentinel
date: 2026-09-08 01:45
status: ส่งมอบ Phase 5 — พร้อมใช้งาน + ล็อก
nvr: 192.168.1.31:8000 NUUO NVRmini 2 NE-2020 fw 03.11.00 Always AutoRecycle 10%
ipc: 192.168.1.21 Seetong LIVE555 gSOAP/2.8 rtsp://admin:123456@192.168.1.21:554/0 1080p
bot: "@hm2569bot hermes" 7346817215 poll 60s + person_watch 30s YOLOv8n 8.4.143
wg: hotel-admin 10.0.0.3/24 → 192.168.1.94:51820 (ต้อง Activate GUI ให้ถาวร)
---

# Hermes Sentinel — เอกสารส่งมอบ (Handover) 2026-09-08

> โครงการ **HMS-2026-001 Hermes Sentinel** — คืนชีพ NVR+IPC ตกรุ่นไร้ Support ด้วย Agent
> ส่งมอบหลังจบ Phase 0–4 → Phase 5 ล็อกระบบ พร้อมใช้งานจริงบน Matebook D2019 (Edge รอเลือก hardware)

---

## 1) สรุปสิ่งที่ส่งมอบ

| รายการ | รายละเอียด | สถานะ |
|---|---|---|
| **NVR** `192.168.1.31` | `NUUO NVRmini 2 NE-2020 fw 03.11.00` `Always Camera1/2 AutoRecycle 10%` เก็บ 24 ชม. วนทับ | ✅ ใช้ต่อ ไม่ต้องเปลี่ยน |
| **IPC** `192.168.1.21` | `Seetong gSOAP/2.8` `RTSP /0 1080p 12fps` `admin/123456` ส่งอย่างเดียว | ✅ สตรีมปกติ |
| **Bot** `Matebook D2019 192.168.1.44` | `bot.py` poll 60s + `health.py` + `person_detect.py` YOLOv8n + `@hm2569bot` | ✅ รันจริง 338 checks ok 100% |
| **Person Filter** | `yolov8n.pt 6.2MB` `ultralytics 8.4.143` สาธิต `bus 3 คน` + `demo_nvr 6 เฟรม 0 คน` (กลางคืน) | ✅ พร้อม `/person` |
| **Edge Plan** | `Phase4-Edge-Deploy-Report.md` แนะนำ Z2W watchdog / Pi4/MiniPC YOLO | 📋 รอเลือก hardware |
| **Remote** | `WireGuard hotel-admin 10.0.0.3/24` + `Cloudflare WARP Connected` กัน CVE-2018-1149 | ⏳ Activate GUI 5 นาที |

---

## 2) ผัง IP และอุปกรณ์

| # | อุปกรณ์ | IP | พอร์ต | Login | หมายเหตุ |
|---|---|---|---|---|---|
| 1 | **NUUO NVRmini 2** | `192.168.1.31:8000` | 8000 web | `admin / admin` | `94:de:80:9d:53:f1` `MaxIPCam 2` `Always 10%` |
| 2 | **Seetong IPC** | `192.168.1.21:554` | 80/554 RTSP | `admin / 123456` RTSP `/0` 1080p `/1` 360p | `00:4a:7b:3e:00:61` `1666840.seetong.com` |
| 3 | **Matebook D2019 Dev** | `192.168.1.44` | — | Windows 10 | รัน bot ปัจจุบัน `poll 60s` `ffmpeg 9.0.1` |
| 4 | **Edge (จอง)** | `192.168.1.33` | — | `pi / …` | `Pi Z2W / Pi4 / MiniPC` เลือกตาม Phase 4 |
| 5 | **WG Server** | `192.168.1.94:51820` | 51820 UDP | `XGpVc…` peer | Gateway `ecs-agent` |
| 6 | **Gateway** | `192.168.1.1` | — | — | Router หลัก |

**ไฟล์ผัง:** `cctv/docs/ip-map.md:1` | `cctv/docs/2026-09-08_Config-Test-Report.md:1`

```
[IPC .21:554/0] --RTSP h264--> [NVR .31:8000] --HDD--> เก็บ Always 10%
        |                           ^
        | (บอทเรียกเมื่อต้องการ)      |
        `----> [Matebook .44 Bot poll 60s] --Telegram--> @hm2569bot → 7346817215
                      |
        [คนนอกบ้าน] --WG 10.0.0.3/24--> [.94:51820] --> LAN .31 Playback
        [Bot → Telegram] --WARP--> api.telegram.org
```

---

## 3) วิธีเปิด WireGuard ให้ถาวร (ค้าง 5 นาที)

> ปัจจุบัน `WireGuardManager Running` แต่ `WireGuardTunnel$hotel-admin` ยังไม่อยู่ — ต้อง Activate ครั้งเดียวแล้ว `fix-wg-auto.ps1` จะซ่อมอัตโนมัติทุก reboot

1. เปิด **WireGuard** (Run as Administrator)
2. เลือก Tunnel **`hotel-admin`** → **Edit**
3. แก้ **Peer Public Key** จาก `cseC7…` → `qmayuuUMsQ/Vc8qddbHdtg3JuyG/fBSUT/5KRmrqa18=` → **Save**
4. กด **Activate**
5. ตรวจ `powershell: wg show` ควรเห็น `latest handshake XXs transfer 1KiB` + `ping 10.0.0.1 1ms 0% loss` + `Test-NetConnection 10.0.0.1:3000 True`
6. หลัง reboot จะมี `Startup\fix-wg-auto.ps1` รัน `wg set hotel-admin peer qmay… allowed-ips 10.0.0.0/24 endpoint 192.168.1.94:51820 persistent-keepalive 25` อัตโนมัติเมื่อ `hotel-admin Up`

> Gateway แก้ถาวรแล้ว `wg0.conf` มี `XGpVc…` — ไม่ต้องแก้อีก

---

## 4) คำสั่ง Telegram `@hm2569bot`

| คำสั่ง | ใช้ทำอะไร | ตัวอย่าง |
|---|---|---|
| `/status` | ดู NVR+IPC + Person backend | `/status` → `NVR ✅ NUUO(200) IPC ✅ rtsp ok Person: YOLOv8n ✅ auto OFF` |
| `/snapshot` | ดึงภาพล่าสุด 1080p | `/snapshot` → รูป `70KB 1920x1080` ส่งกลับทันที |
| `/clip [วินาที]` | ดึงคลิป 2–30 วิ | `/clip 10` → วิดีโอ 10 วิ (copy → re-encode ถ้าไม่มี keyframe) |
| `/person [conf]` | สแกนหาคนด้วย YOLO | `/person 0.5` → `พบคน 1 คน bbox` หรือ `ไม่พบคน backend=yolo` |
| `/person_auto [on/off]` | เปิด/ปิด auto scan ทุก 30 วิ (cooldown 5 นาที) | `/person_auto on` → เจอคนส่งรูปอัตโนมัติ |
| `/reboot` | สั่ง power cycle (ต้องต่อ Smart Plug) | `/reboot` → placeholder รอ `actions.py` |

**Chat ที่อนุญาต:** `7346817215 (lnw)` — ตั้งใน `config.yaml: allow_chat_ids`

---

## 5) Bot บน Matebook — วิธีดูแล

### รันอยู่ตอนนี้

- `bot.py` `poll 60s` + `person_watch 30s` → `cctv/scripts/pi-z2w-bot/status.db` **338 records ok 100%** (ล่าสุด `2026-09-08T01:48:41 ipc rtsp ok`)
- ภาพ hourly `snapshot-hourly.ps1` → `cctv/output/snapshot_*.jpg` (68–70KB)
- Auto-start: `Startup\HermesSentinel-Bot.bat` + `Startup\fix-wg-auto.ps1`

### ถ้าบอทดับ — สั่งรันใหม่

```powershell
# ตรวจ
Get-Process python -ErrorAction SilentlyContinue | Format-Table Id,ProcessName
python -X utf8 cctv/scripts/pi-z2w-bot/health.py   # ควรได้ NVR True NUUO(200) IPC True rtsp ok

# รันใหม่
Start-Process python -ArgumentList "cctv/scripts/pi-z2w-bot/bot.py" -WindowStyle Hidden
# หรือดับเบิลคลิก cctv/scripts/pi-z2w-bot/start-bot.bat
```

### ไฟล์สำคัญ (ห้าม commit — อยู่ใน .gitignore)

| ไฟล์ | อยู่ที่ไหน | สำรองอย่างไร |
|---|---|---|
| `config.yaml` (token `874…` + pass `admin/123456`) | `cctv/scripts/pi-z2w-bot/config.yaml` | **ห้าม push** — ก๊อปไว้ที่ `C:\Backup\TCCcom\config-2026-09-08.yaml` + ไดรฟ์ส่วนตัว |
| `status.db` (338 checks) | `cctv/scripts/pi-z2w-bot/status.db` | ก๊อปไว้คู่กับ config — ใช้ดูย้อนหลัง `sqlite3 status.db "select * from checks order by ts desc limit 20"` |
| `yolov8n.pt 6.2MB` | `C:\…\T.C.Com\yolov8n.pt` | ไม่ต้องสำรอง — โหลดใหม่ `pip download ultralytics` ได้ |
| ภาพตัวอย่าง | `cctv/output/demo_nvr/*.mp4` `cctv/output/person/*.jpg` | อยู่ใน `.gitignore` — ก๊อปแยกถ้าต้องการส่งลูกค้า |

**ต้นแบบที่ commit:** `config.yaml.example:1` — ใช้สร้าง `config.yaml` ใหม่ได้

---

## 6) Person Filter — วิธีใช้จริง

### บน Matebook (พร้อมแล้ว)

```powershell
# สแกนภาพนิ่ง
python -X utf8 cctv/scripts/pi-z2w-bot/person_detect.py --image cctv/output/snapshot_2026-09-08_00-40.jpg

# สแกนสด RTSP
python -X utf8 cctv/scripts/pi-z2w-bot/person_detect.py --rtsp rtsp://admin:123456@192.168.1.21:554/0 --conf 0.5

# สแกนคลิปย้อนหลัง
python -X utf8 cctv/scripts/pi-z2w-bot/person_detect.py --clip cctv/output/demo_nvr/nvr_clip_10s_re.mp4
```

- ถ้าติดตั้ง `ultralytics+torch` → `backend yolo` จับคนแม่น `conf 0.5`
- ถ้าไม่มี → `motion fallback` (ใช้ไม่ได้กับ YOLO, เช็ค `ipcam_event.php` ของ NVR แทน)

### สาธิตที่ทำแล้ว (ของจริง)

| ไฟล์ | ผล YOLO | หมายเหตุ |
|---|---|---|
| `demo_nvr/frames_re/frame_001-006.jpg` (6 เฟรม 60–62KB) | **0 คน** `conf>0.5` | กลางคืนว่างถูกต้อง — `conf 0.3` เจอ 1 FP |
| `bus.jpg` → `person_20260908_010710.jpg 345KB` | **3 คน** `0.85` | Positive control |
| `live scan_snapshot RTSP` | **0 คน** | ตรงกับคลิป |
| `cctv/output/person/_last_noperson.jpg 67KB` | debug ว่าง | |

**วิธีได้ภาพมีคนจริง:** ตอนกลางวันให้คนยืนหน้า `192.168.1.21` แล้วสั่ง `/person` หรือ `/person_auto on` — จะได้ `person_*.jpg` กรอบเขียวใน `cctv/output/person/`

---

## 7) ย้ายไป Edge (เมื่อพร้อม)

> ตาม `Phase4-Edge-Deploy-Report.md:1` — Z2W 512MB ไม่พอ YOLO ต่อเนื่อง

| ตัวเลือก | เหมาะกับ | YOLO |
|---|---|---|
| **A Z2W** | watchdog `/snapshot /clip` ประหยัดไฟ | fallback motion |
| **B Pi 4 4GB** | `person_auto 30s` จริง | ~2–3s/เฟรม |
| **C MiniPC** | ดีสุด SSD ทน | <1s/เฟรม |

```bash
# เมื่อเลือก hardware แล้ว (192.168.1.33)
scp -r cctv/scripts/pi-z2w-bot pi@192.168.1.33:~/cctv-bot/
ssh pi@192.168.1.33
  cd ~/cctv-bot; cp config.yaml.example config.yaml  # เติม token/pass เอง
  pip install -r requirements.txt      # B/C เพิ่ม ultralytics
  python3 health.py                    # ตรวจ rtsp ok
  sudo cp cctv-bot.service /etc/systemd/system/
  sudo systemctl daemon-reload && sudo systemctl enable --now cctv-bot
  sudo systemctl status cctv-bot; journalctl -u cctv-bot -f
```

ต้องเตรียม `High-Endurance SD 32GB + USB-Ethernet + log2ram` กัน SD พัง

---

## 8) ความเสี่ยงและวิธีกัน

| เสี่ยง | กันอย่างไร |
|---|---|
| `NVR fw 03.11.00` `CVE-2018-1149` | **ห้ามเปิด 8000 ออก WAN** — เข้าผ่าน `WireGuard 10.0.0.3/24` เท่านั้น + `WARP Connected` |
| `Matebook ดับ` | `Task Scheduler + Startup\HermesSentinel-Bot.bat` auto-start — NVR ยังเก็บ `Always 10%` ต่อ |
| `SD Edge พัง` | `High-Endurance + log2ram + overlay` |
| `บอทแจ้งถี่` | `fail 3× + cooldown 15 นาที` ใน `bot.py:138` + `person cooldown 5 นาที` |
| `WireGuard หลุด` | `fix-wg-auto.ps1` auto-fix ทุก reboot (ต้อง Activate ครั้งแรกก่อน) |

---

## 9) ตรวจสอบส่งมอบ (Verification)

```powershell
python -X utf8 verify_system.py          # ต้อง ALL PASS 10/10
python -X utf8 rebuild_index.py          # อัปเดต index.md
python -X utf8 cctv/scripts/pi-z2w-bot/health.py  # NVR True NUUO(200) IPC True rtsp ok
Get-Content cctv/output/handover-2026-09-08.md | Select-String "hqq|Phase"
git status                               # เหลือ curated เท่านั้น
git check-ignore -v cctv/scripts/pi-z2w-bot/config.yaml cctv/scripts/pi-z2w-bot/status.db yolov8n.pt cctv/output/demo_nvr/nvr_clip_10s_re.mp4
```

**ผลล่าสุด 2026-09-08 01:48:** `verify_system.py ALL PASS 10/10` + `status.db 338/338 ok 100%` + `health.py rtsp ok` + `secrets ignored PASS`

---

## 10) ไฟล์ที่เกี่ยวข้อง

- `cctv/plans/2026-09-08_Master-Execution-Plan.md:91` (Phase 5 ส่งมอบ)
- `cctv/plans/Project-Hermes-Sentinel.md:58`
- `cctv/plans/Phase4-Edge-Deploy-Report.md:1`
- `cctv/plans/Phase3-NVR-Demo-Report.md:1` + `Phase3-Person-Report.md:1`
- `cctv/plans/Phase2-LongRun-Report.md:1`
- `cctv/docs/2026-09-08_Config-Test-Report.md:1`
- `cctv/docs/ip-map.md:1`
- `cctv/scripts/pi-z2w-bot/README.md:1` `bot.py:1` `health.py:1` `person_detect.py:1`

---

**ผู้ส่งมอบ:** Hermes Sentinel (HMS-2026-001) — Matebook D2019 bot + NVR/IPC ตกรุ่น  
**ผู้รับ:** T.C.Com — เก็บ `config.yaml + status.db` ที่ปลอดภัย, เปิด WG ให้ถาวร, เลือก Edge เมื่อพร้อม YOLO auto  
**วันที่:** 2026-09-08 01:45 — Phase 5 DONE ล็อกระบบ

